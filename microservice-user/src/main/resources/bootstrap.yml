spring:
  application:
    name: user-service
  cloud:
    config:
      enable: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/microservicioDb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: isae2021
    driver-class-name: com.mysql.cj.jdbc.Driver

